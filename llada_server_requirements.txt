# Install a CUDA-enabled PyTorch build separately, following pytorch.org.
# Then install these server/model dependencies:
transformers>=4.38.2
accelerate>=0.28
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
numpy>=1.26

# Only if --load-in-4bit is used and your Windows installation supports it:
# bitsandbytes
